{
  "name": "filesync",
  "version": "0.1.0",
  "private": true,
  "description": "Serverless file sync + ephemeral chat tool on Cloudflare Workers",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @filesync/backend dev",
    "dev:frontend": "cd packages/frontend && pnpm run dev",
    "build:frontend": "cd packages/frontend && pnpm run build",
    "deploy": "pnpm --filter @filesync/backend deploy",
    "test": "pnpm --filter @filesync/backend test",
    "lint": "pnpm -r lint"
  }
}
