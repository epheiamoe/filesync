# filesync — Architecture & Current State

> Last updated: 2026-06-22 after commit `0e63179`

## Overview

filesync is an **end-to-end encrypted (E2EE) file sync + ephemeral chat tool** running on Cloudflare's serverless platform. The server is a **blind proxy**: it never sees plaintext content. All encryption/decryption happens client-side via Web Crypto API (AES-256-GCM).

## Stack

### Backend (`packages/backend`)
| Layer | Technology |
|-------|-----------|
| Runtime | Cloudflare Workers |
| Framework | Hono (lightweight router) |
| WebSocket | **Hibernatable Durable Objects** (one RoomDO instance per room) |
| Database | D1 (SQLite-based, schema at `db/schema.sql`) |
| Files | R2 (object storage, chunked upload) |
| Sessions / Config | KV (TTL-based expiry, no cleanup needed) |
| Cron | Wrangler Cron Triggers (hourly cleanup via `scheduled()`) |
| Auth | Cookie (`epheia_session` HttpOnly) + `Authorization: Bearer` fallback |
| Validation | Zod |
| Testing | Vitest + `@cloudflare/vitest-pool-workers` |
| Typescript | v5.4+ |

**Bindings:** `DB` (D1), `KV` (KV), `FILES` (R2), `RoomDO` (DO namespace).

### Frontend (`packages/frontend`)
| Layer | Technology |
|-------|-----------|
| Framework | React 18 |
| Router | React Router v6 |
| State | Zustand |
| Styling | Tailwind CSS v3 |
| Animations | Framer Motion |
| Crypto | Web Crypto API (AES-256-GCM) |
| QR | qrcode + jsqr |
| PWA | vite-plugin-pwa |
| Bundler | Vite |
| Typescript | tsc --noEmit |

### Shared (`packages/shared`)
All TypeScript type definitions (DTOs, request/response shapes) shared between backend and frontend.

## E2EE Model

```
Client A                          Server (Blind Proxy)                     Client B
  |                                     |                                     |
  |-- AES-256-GCM encrypt(content, key) |                                     |
  |-- POST /api/chat/messages --------->|                                     |
  |    { encrypted_content: "base64..."} |                                     |
  |                                     |-- Store in D1 (ciphertext only)     |
  |                                     |-- Broadcast via RoomDO WS --------->|
  |                                     |                                     |-- AES-256-GCM decrypt(content, key)
```

### Key Properties
- **32-byte random key** per room, generated by room creator
- Key encoded as **Crockford base32** (avoids I/L/O/U confusion), grouped in 4-char chunks
- **SHA-256 hash** of key sent to server for join verification (server never sees raw key)
- Share string format: `{room_code}-{key_base32_groups}`
- Files: client encrypts **before** upload; server stores ciphertext in R2
- Private files: encrypted on disk; server sets `X-File-Encrypted: 1` header
- Public files: stored unencrypted in R2 for direct URL access; accessible without auth via `/api/files/:id/public`

## Key Design Decisions

### 1. Hibernatable DO WebSocket (one-per-room)
Why: Cloudflare's hibernatable WebSocket API minimizes billing. The DO sleeps when no messages are in flight and wakes only on `webSocketMessage`, `webSocketClose`, `webSocketError`, or HTTP fetch. Multiple clients in the same room share one DO instance via `idFromName(roomCode)`.

### 2. Cookie + Bearer Auth
Auth middleware checks `epheia_session` cookie first (browser sessions), then falls back to `Authorization: Bearer` header (API/CLI). Three login methods: admin password, API key, temp credential (6-char alphanumeric code).

### 3. client_fingerprint
A persistent random string (`localStorage`-backed) sent with JoinRoom requests. Enables cross-session room member tracking without storing PII.

### 4. TTL-Based Auto-Cleanup
- **Messages**: optional `ttl_seconds` field (10s – 24h) for burn-after-read
- **Files**: `expires_at` ISO 8601 timestamp
- **Rooms**: configurable inactivity TTL (KV key `config:roomTtlHours`, default 24h)
- Hourly cron (`scheduled()`) cleans expired files, inactive rooms, burnt messages, expired credentials, and orphan R2 objects

### 5. Chunked File Upload
- `POST /api/files/upload/init` → get upload_id + R2 key + chunks_needed
- `POST /api/files/upload/part` (multipart) → upload each chunk
- `POST /api/files/upload/complete` → assemble chunks, store metadata in D1
- `POST /api/files/upload/abort` → cancel upload

### 6. WS Ticket Pattern
WebSocket connections use a **two-step** approach:
1. `GET /api/ws?room=XXXX&token=YYY` → returns a 60s-valid ticket (stored in KV)
2. `GET /api/ws/connect?ticket=XXX` → upgrade to WebSocket via DO

This isolates auth from the WebSocket upgrade handler.

## Data Flow (Message Send)

```
[Client] encrypt → POST /api/chat/messages → [Worker]
  → INSERT into D1 (ciphertext)
  → roomStub.fetch('/internal/broadcast') → [RoomDO]
  → DO iterates all connected WebSocket tags → sends event to each client
  → [Other clients] decrypt & display
```

## Recent Fixes Summary (since commit `33ddabe`)

| Commit | Category | Fixes |
|--------|----------|-------|
| `8bbc8c6` | DTO mismatch | Frontend used `wsMessage.payload` but WS sent `data` — fixed field name alignment |
| `f34afef` | Content-Type | Removed duplicate `writeHttpMetadata` that overrode `Content-Type` for View mode |
| `a29dea4` | WS/auth | Fixed WS 403 race condition (ticket expiry before connect), silent message send failure, added share button |
| `0d2a89d` | File decrypt | Client-side decrypt for View/Download, `addFile` dedup, HTTP fallback for file fetch |
| `d77e17f` | Chat UI | Countdown circle for burn-after-read, delete animation (DestroyAnimation) |
| `76fb70d` | Crypto/QR | Fixed key encoding in share strings, WS recall event structure, QR dual-code display, QR scanner, fingerprint |
| `6186b09` | Backend infra | D1 migrations, client_fingerprint, TTL, hash integrity, cron cleanup, admin config |
| `8c1165d` | WS sync | 6 fixes for missing WS events, dropped fields, silent failures |
| `ead7375` | Frontend | Chat order (ASC), countdown timer, file sync debug logs, public sharing URL |
| `f0167be` | Chat layout | flex-col + justify-end + min-h-full for Telegram-style layout |
| `7abc022` | Chat layout | ASC sort + flex-1 spacer for messages-at-bottom layout |
| `0e63179` | QR/share | `encodeURIComponent` for share links, independent copy states per field, hash-based auto-login |

## Deployment

| Resource | URL |
|----------|-----|
| Backend Worker | `https://filesync-api.epheia.workers.dev` |
| Frontend Pages | `https://epheia-files.pages.dev` |
| D1 Database | `filesync-db` |
| R2 Bucket | `filesync` |
| KV Namespace | `EPHEIA_FILES_KV` |

## Database Schema

Defined in `packages/backend/db/schema.sql`. Key tables:
- `admin_accounts` — single admin account
- `sessions` — KV-backed (not in D1)
- `rooms` — `id`, `room_code`, `created_at`, `last_active_at`
- `room_members` — `room_id`, `session_id`, `device_label`, `client_fingerprint`
- `messages` — `id`, `room_id`, `encrypted_content`, `message_type`, `ttl_seconds`, `expires_at`, `recalled_at`
- `files` — `id`, `room_id`, `r2_key`, `encrypted_filename`, `encrypted_meta`, `file_hash`, `visibility`, `expires_at`, `recalled_at`
- `temp_credentials` — `code`, `expires_at`, `label`
- `api_keys` — `key_hash`, `label`
